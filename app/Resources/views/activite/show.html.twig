{% extends 'AlterEgoBundle:Worker:layout_worker.html.twig' %}
{% block head %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <h1 class="title_activity">Activite</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ activite.activite }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ activite.type }}</td>
            </tr>
            <tr>
                <th>Info</th>
                <td>{{ activite.info }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('activite_index') }}">Retour Accueil</a>
        </li>
        <li>
            <a class="btn" href="{{ path('activite_edit', { 'id': activite.id }) }}">Edit</a>
        </li>
        <a class="btn" href="{{ path('creneau_new', { 'id': activite.id }) }}">Ajouter un créneau</a>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
