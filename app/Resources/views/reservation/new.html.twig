{% extends 'AlterEgoBundle:Worker:layout_worker.html.twig' %}

{% block body %}
    <h1>Reservation creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Valider Ma réservation" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('reservation_index', {'id': seance.0.id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}

