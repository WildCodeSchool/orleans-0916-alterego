{% extends 'AlterEgoBundle:Worker:layout_worker.html.twig' %}
{% block head %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <h1 class="title_activity">Activite</h1>
    <table class="table-condensed table-responsive table-striped">
        {{ dump(seance) }}
        <tbody>
        <tr>
            <th>Activite</th>
            <td>{{ activite.activite }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ activite.type }}</td>
        </tr>
        <tr>
            <th>Info</th>
            <td>{{ activite.info }}</td>
        </tr>
        </tbody>
    </table>
    {%   render controller('AlterEgoBundle:Reservation:new') %}
{% endblock %}
