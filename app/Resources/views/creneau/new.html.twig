{% extends 'AlterEgoBundle:Coach:layout_coach.html.twig' %}

{% block content %}
    <h1>Creneau creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('creneau_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
