{% extends 'AlterEgoBundle:Worker:layout_worker.html.twig' %}
{% block head %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <h1 class="title_activity">Photo de Profile</h1>

    <table>
        <tbody>
        <tr>
            <th>Visualisation</th>
            <td><img src="{{ asset('uploads/images/' ~ image.path ) }}"/></td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}